import React from "react";

const SeparatorSection = () => {
  return (
    <section className="h-40 bg-white w-screen text-6xl flex items-center justify-center italic font-roxborough tracking-tighter">
      Empowering the Future with Clean, Sustainable Energy.
    </section>
  );
};

export default SeparatorSection;
